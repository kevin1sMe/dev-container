version: '3'
services:
  dev:
    image: mirrors.tencent.com/red/kv-dev:latest
    volumes:
      - '/var/run/docker.sock:/var/run/docker.sock'
      - "/:/rootfs"
      - "/root/.vscode-server/:/root/.vscode-server/"
    privileged: true
    network_mode: "host"
